// Place your settings in this file to overwrite default and user settings.
{
    "files.exclude": {
        "node_modules":{},
        "aspnet_client":{},
        "**/*.map": {},
        "**/*.js": {
            "when": "$(basename).ts"
        },
        "**/*.css": {
            "when": "$(basename).scss"
        }
    }
}